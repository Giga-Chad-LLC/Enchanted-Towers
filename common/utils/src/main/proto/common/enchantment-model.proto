syntax = "proto3";

option java_package = "enchantedtowers.common.utils.proto.common";
option java_multiple_files = true;

package common_models;

message EnchantmentModel {
  // this enum is related to spells that compose enchantment
  // TODO: update the message to describe Enchantment correctly

  enum ElementType {
    EARTH = 0;
    AIR = 1;
    FIRE = 2;
    WATER = 3;
  }

  ElementType element = 1;
};
